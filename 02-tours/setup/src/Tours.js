import React from 'react';
import Tour from './Tour';
const Tours = ({props, removeTour}) => {
  return (
    
        <Tour props = {props} removeTour = {removeTour}></Tour>

  );
};

export default Tours;
